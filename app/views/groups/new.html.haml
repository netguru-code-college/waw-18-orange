= simple_form_for @group do |f|
  = f.input :name
  = f.input :description
  = f.input :total_amount, as: :decimal
  = f.association :members, collection: User.where.not(id: current_user.id), as: :check_boxes, label_method: :username
  = f.button :submit
